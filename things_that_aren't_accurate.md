# Things that don't quite work in the setup docs

- Building and viewing the documentation
  - Neither `docker-compose up builddocs` nor `docker-compose up -d viewdocs` work, as docker will say `no such service: ____`
